#!/usr/bin/env bash
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"

python3 tools/scripts/baseline_profile_guard.py \
  --staged \
  --fail-when-outdated \
  --quiet || exit 1

exit 0
