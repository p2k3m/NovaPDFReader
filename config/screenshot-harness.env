# Default environment for the screenshot harness tooling.
#
# Provides the test application package so the harness can resolve the
# instrumentation target deterministically without relying on device state.
NOVAPDF_SCREENSHOT_TEST_PACKAGE=com.novapdf.reader.test
